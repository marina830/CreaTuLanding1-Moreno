export default function ItemListContainer({ greeting }) {
  return (
    <section className="hero">
      <h1>{greeting}</h1>
      <p>Pronto vas a ver aquí el catálogo de productos.</p>
    </section>
  );
}
